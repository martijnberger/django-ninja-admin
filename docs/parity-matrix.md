# Django API Admin v1.3.0 Parity Matrix

This matrix tracks parity against `daemon-bixia/django-api-admin` 1.3.0 and Django-admin behavior while keeping the v2 Ninja/Pydantic API contract. It is intentionally semantic: JSON envelopes and DRF-specific extension points are not required to match.

Statuses:

- `implemented`: covered by current code and local tests.
- `partial`: a working foundation exists, but important upstream/Django-admin semantics remain.
- `missing`: no meaningful implementation yet.
- `changed`: intentionally different in v2.

## Package And Public API

| Area | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Distribution/import package | implemented | `pyproject.toml`, `django_ninja_admin` package | Release metadata hardening in Phase 7 |
| Public exports | implemented | `django_ninja_admin/__init__.py` exports site/admin/filter/decorator APIs and `ShowFacets` | Keep stable as new hooks land |
| No DRF/drf-spectacular runtime dependency | implemented | Dependencies in `pyproject.toml`, `tests/test_package_contract.py`, `scripts/package_smoke.py` | Keep smoke coverage in CI |
| DRF serializer hooks | changed | Intentionally unsupported; replaced by Ninja-native `form_class`, `output_schema`, and `schema_field_overrides` hooks in `docs/api-and-auth.md` | Expand examples as hooks grow |

## Site Routes

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| `GET /apps` and `GET /apps/{app_label}` | implemented | `NinjaAdminSite.get_app_list`, API tests, and typed auth-error OpenAPI maps | Upstream fixture comparisons |
| `GET /context` | implemented | `NinjaAdminSite.each_context`, API tests, custom metadata tests, and `has_permission()` override coverage | Upstream fixture comparisons |
| `GET /permissions` | implemented | Site route in `sites.py`, site-level `has_permission`, registered-model permission summaries, default/auth-none/custom-hook API tests, and typed auth-error OpenAPI maps | Upstream fixture comparisons |
| `GET /history` | partial | History route, log model, `tests/test_history.py`, app/model/object/action filter, model/object-level permission-filter tests, client-controlled page/page-size pagination with max bounds, page-scoped object-visibility filtering, per-row model identity/object links, and Django-style change-message text | Broader upstream history fixture comparisons |
| `GET /autocomplete` | partial | Permission-hardened route, `tests/test_autocomplete.py`, rich pagination metadata, remote `get_paginator()` and `get_search_fields()` hook coverage, page-scoped object-level remote view-permission result filtering, many-to-many source-field tests, non-PK `ForeignKey(to_field=...)` option IDs, source-model access checks, declared-field checks, and source-field `limit_choices_to` filtering/form metadata coverage | Additional remote-field edge cases |
| `GET /view-on-site/{content_type_id}/{object_id}` | implemented | Route, permission, callable hook, external URL tests, configured Site-domain URLs, and request-host fallback when Site is missing | Upstream fixture comparisons |
| Docs/OpenAPI routes | partial | `tests/test_openapi_contracts.py` covers broad OpenAPI component examples, route request/response maps, multipart schema hints, and built-in route contracts; `/docs`, `/openapi.json`, semantic model-route contract tests, generated JSON mutation examples, generated per-model component examples, built-in site-route success examples, auth-aware built-in/model route maps, alternate custom-action success maps, custom-route success/error response maps, `just openapi-diff` contract-review tooling, and `just generated-client-smoke` OpenAPI consumer coverage across core model and site route groups | Broader model-route snapshots |

## Registry, Model Admins, And Checks

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Model registration/unregistration | implemented | `NinjaAdminSite.register`, `unregister`, duplicate/unregistered/abstract/swapped/decorator tests | Upstream fixture comparisons |
| Default site/autodiscover | implemented | Lazy `site`, `autodiscover()`, sample-project smoke, and partial-import rollback test | Upstream fixture comparisons |
| Permission hooks | partial | `BaseAdmin.has_*_permission`, model permission summaries, custom model permission hook tests, changelist row permission metadata, object-level change-form permission metadata, history/autocomplete/action/bulk/delete object-level tests, and inline permission tests | Broader upstream object-permission fixture comparisons |
| Admin system checks | partial | `django_ninja_admin/checks.py`, invalid-admin, empty/callable/relation-path/many-to-many/reverse relation `list_display`, action shape/permission-hook checks, `list_display_links` item-type checks, `list_editable` item-type, form-layout, first-column link, and duplicate-field conflicts, inline shape/count/range/boolean/form-layout item/fieldset item/formset/parent-FK checks, row-tuple `fields`, duplicate `fields`/`exclude`/`readonly_fields`/`list_display_links`, callable readonly field layout names, manual-through m2m form-layout checks, fieldset shape/duplicate checks, `fields`/`exclude` item checks, `prepopulated_fields`, `sortable_by`, ordering random-marker/expression checks, field-based `list_filter` tuple/class checks, `date_hierarchy` type/path/field checks, pagination type/range/paginator options, boolean/save/action/full-count/view-on-site/facet options, search help text, empty value display, custom `form_class`, `formfield_overrides`, `schema_field_overrides`, `list_select_related`, `list_prefetch_related`, autocomplete target registration/searchability, widget-conflict, reverse relation widget, and manual-through m2m widget tests | Match Django check IDs/coverage more closely |
| `get_changelist()` hooks | implemented | `ModelAdmin.get_changelist*`, route hook test | More subclassing examples/docs |
| Custom site/model views | partial | `admin_view()`, `get_urls()`, direct and decorator-style `route()`, route tags/descriptions, hidden routes, raw method wrapping, `auth=None`, named response schemas, default JSON-object response schemas, generated/explicit route operation IDs with multi-method uniqueness, typed error response maps, custom route tests, and site/route-level auth-sequence route tests | Deeper override-hook parity and upstream fixture comparisons |
| Display decorator metadata | partial | `@display` descriptions, ordering, boolean flags, empty values, callable `list_display`, model-property metadata in changelist tests, and callable readonly-field metadata including ordering hints | More readonly-field display variants |

## Changelist, Filtering, Search, And Ordering

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Shared changelist queryset for list/actions | implemented | `ChangeList`, filtered action test, and action UI metadata tests | More selected/action edge cases |
| Search fields | partial | `ModelAdmin.get_search_results`, active search metadata and clear links, many-to-many duplicate distinct tests, prefix lookup tests, and non-text `__exact` cast tests | Broader upstream fixture comparisons |
| Lookup validation | partial | `lookup_allowed`, changelist `_to_field` validation/row identity tests, direct repeated/comma-separated `__in` and `__isnull` lookup preparation, local lookup-suffix tests, `limit_choices_to` relation-lookup tests, invalid lookup-key tests, and invalid lookup-value typed error tests | Match remaining Django suspicious lookup edge cases |
| Pagination/show all | implemented | `ChangeList`, shared `Pagination` schema and site pagination payload helpers, pagination/show-all/last-page tests, presence-style `all` parameter behavior, custom paginator hook coverage, row/result indexes, page-result/range metadata, structured page choices, first/previous/next/last and show-all query strings, admin-style page range metadata, and `show_full_result_count` tests | Large-result behavior |
| Ordering/sort links | partial | Sort metadata plus default-ordering, visible custom queryset ordering, and multi-column ordering state/link tests, deterministic primary-key fallback ordering, stale-page reset in generated links, pagination query strings, and preservation of unrelated lookup params such as `price__gte` | Deeper Django query-string semantics |
| `list_select_related` and relation query optimization | partial | `auto_select_related_fields`, direct-FK, relation-path, callable related-ordering query-count tests, explicit string/`Prefetch` `list_prefetch_related` support, and callable many-to-many display query-count tests | More inferred prefetch strategy and broader query-count coverage |
| List display links/display metadata | partial | Changelist column metadata, row-level cell metadata, callable display columns, single-valued relation-path columns, plus row index, URL, and object-permission metadata tests | More readonly-field action variants and upstream fixture comparisons |
| List filters | partial | Package-owned filter classes, bounded date-range filter tests, choices/all-values null tests, strict invalid `__isnull` tests, empty-filter validation tests, related-filter/simple-filter visibility tests, non-PK `to_field` related-filter value tests, many-to-many empty relation tests, related-only ordering tests, stale-page reset in generated choices, filter-state and clear-all metadata, and common filter tests | Semantic edge-case comparison against Django/upstream |
| Facets | partial | `_facets=1`, `ShowFacets.NEVER`/`ALLOW`/`ALWAYS` route tests, optionality metadata, and add/remove facet query strings | Query-count optimization and exact Django facet semantics |
| `date_hierarchy` | partial | Date hierarchy metadata/filter tests, relation-path date fields, field-type/timezone metadata, bounded range filtering including max-year bounds, active-timezone DateTime bucketing, clear/back navigation query strings, stale-page reset in generated links, lowest-useful initial level selection, selected day state, and invalid date validation | Additional date edge cases and deeper Django navigation parity |

## Forms, Fields, And Serialization

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Dynamic output schemas | partial | `BaseAdmin.get_output_schema`, non-null typed primary keys, blank/non-null model fields, explicit JSON value schemas, decimal precision constraints, bounded numeric model-field constraints including strictest min/max validator collapse for relation target schemas, zero-offset step validators, string length validators, email/URL/IP format metadata, base64 binary-field output metadata, model regex/slug patterns, typed file/image/relation/many-to-many/choice fields including constrained many-to-many target items and target-aware examples, JSON-compatible changelist row IDs/cells/cell metadata, list-editing row PKs, and history identifiers/change messages, `output_exclude` schema/serialization filtering, Ninja `register_field()` mappings for custom primary keys/relation output IDs/examples, and schema override tests including computed `ModelAdmin` methods plus valid examples for common explicit constrained scalar/container override types | Stable component names and richer custom fields |
| Pydantic request envelopes | partial | `tests/test_form_field_schemas.py` covers richer form-field value schemas, parent/bulk/inline form schema overrides, and typed choice coercion; Per-model create/update/bulk, relation inputs typed from target primary key or `to_field_name` including Ninja `register_field()` custom target fields and target string constraints, unknown parent-field rejection, explicit parent/inline/bulk `form_schema_field_overrides` with validating component and route-level examples, reusable scalar object-identifier schemas for action selections, bulk row PKs, and inline change/delete IDs, richer JSON-compatible value schemas/UUID/IP/email/URL/duration/date-time-input-format/split-datetime/generic-multivalue/file-path/combo-field/file-image/null-boolean/multiple-choice/typed-choice form-field types, enum-style concrete/grouped/plain-non-JSON-scalar/typed/float/decimal/UUID choices, string/form-validator/numeric-validator/field-regex/validator-regex/numeric/decimal/step constraints including Django-style stripping, disabled parent/inline field optionality, per-inline operation, discriminated action payload variants, and custom action input schemas with OpenAPI tests | Broader edge-case schemas and snapshots |
| Django ModelForm validation | implemented | Create/update tests, including Pydantic-cleaned temporal, URL, IP address, and UUID values bound into custom Django form fields | More field/widget variants |
| Custom `form_class` and formfield hooks | partial | Mounted custom-form and custom-formfield route tests cover schema fields, custom widget attrs/media, inline form classes, inline formfield hooks/media, `formfield_overrides`, `form_schema_field_overrides` request schemas/form metadata, `formfield_for_dbfield`, relation/choice hooks, form validation, persistence, and system checks for `form_class`/`formfield_overrides`/input schema overrides | More field override edge cases |
| Form descriptions | partial | Typed `FieldAttributes` contract, named nested metadata components for relation widgets, filtered selects, radio fields, prepopulated sources, input-schema overrides, two-item choice pairs, choice options, grouped choices, JSON-compatible raw/coerced choice values, validator details, widget attributes, subwidgets, indexed input formats, select-date choices, combo-field children, top-level form/inline prepopulated maps, form radio-field maps, and scalar metadata values such as defaults, initials, current values, empty values, numeric bounds, and step hints, widget/custom-widget intent, disabled-field metadata, normalized parent/inline fieldset layout metadata, inline formset prefixes/management forms/empty forms/row metadata, nullable booleans, semantic subwidget/input-format hints, clearable file intent/current-file metadata, `SelectDateWidget`, temporal input formats, microsecond-support flags, form media, validator names/details, effective validator-derived string/numeric bounds, error-message, `empty_value`, localization, explicit input schema overrides, initial values, structured relation/autocomplete, selected relation labels, relation target-field metadata, mount-aware autocomplete/raw-id endpoint metadata, relation `limit_choices_to`, string stripping, numeric, decimal/step, flat/grouped/typed-coerced choice metadata with typed raw values, file-path/combo-field, readonly display/callable including explicit layout names and ordering hints, model-field identity, file/image, m2m, admin-widget metadata tests, and explicit coverage that rendered Django `BoundField`/widget internals are omitted | Custom field metadata and specialized widget details |
| File/image fields | partial | `tests/test_file_fields.py`, `tests/test_model_field_schemas.py`, and mounted route tests cover FileField output schema/serialization, ImageField typed metadata, current-file form metadata including storage backends without URLs, file-extension upload metadata, typed string-or-null JSON write schemas, JSON clear tests for files/images, multipart create/update routes including required file-field payloads, Pydantic validation for multipart JSON parts, and mounted ImageField invalid/valid upload tests with dimension metadata | Deeper storage/widget edge cases |
| Many-to-many fields | partial | Pydantic write schema, typed output schema, selected-label form value metadata, output serialization, create/update tests | Through models, permissions, and richer dual-select widget semantics |
| Raw ID/radio/filter-horizontal/prepopulated | partial | Per-field `admin_widget`, structured raw-id lookup metadata with related changelist URL/query hints and target-field metadata, structured filtered-select metadata including dual-select direction/stacking, related-model identity, and target-field metadata, structured radio metadata, structured prepopulated source metadata, named OpenAPI components for those nested metadata shapes, and conflict-check tests | Full Django widget rendering semantics and more edge cases |

## Mutations, Inlines, Delete, And Logs

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Create/update/delete routes | partial | `tests/test_crud_routes.py` covers mounted add/change/delete/history CRUD smoke plus route-level form metadata; CRUD/history tests plus mounted save-form/save/delete/response hook, declared schemas for custom add/change/delete `Status` responses, typed mutation success response maps, and allowed/bad `_to_field` detail/update/delete tests | More response hook edge cases |
| Inline add/change/delete | partial | Formset implementation, typed inline schemas, custom inline form and formset support/checks, custom inline multivalue normalization, dynamic inline count validation plus extra-form metadata, row-indexed server errors, max/delete/duplicate/conflict/unknown-object/rollback/permission/unknown-key/unknown-field/readonly-field tests | Deeper upstream formset edge cases |
| Bulk list-editable update | partial | `tests/test_actions_bulk.py`, strict row schema, duplicate-PK rejection, filtered-queryset row limiting, `_to_field` row identity support, dedicated changelist form hook coverage, structured list-editing formset prefix/management/row metadata, unchanged-row skip, empty change-log skip, row-indexed validation and permission errors, and all-rows-before-write tests | Remaining changelist formset edge cases |
| Default delete action/actions | partial | `tests/test_actions_bulk.py`, `delete_selected`, permission-filtered action metadata, custom return, empty-selection, invalid selected IDs, select-across, action permission-hook checks, object-level custom-action permission checks, protected response tests, object-level delete permission details, and `select_across` delete permission filtering | Additional permission edge coverage |
| Protected delete | partial | Direct and action delete return protected/perms details, deleted-object tree details, including object-level delete hook denials | Exact Django protected-object presentation |
| Log entries/change messages | partial | `tests/test_crud_routes.py` covers direct CRUD history messages and no-op direct update skips; Field-label, no-op direct update skip, Django-style human-readable history text, and inline add/change/delete tests, including deleted inline object text | Broader upstream message fixture comparisons |

## Errors, Auth, And OpenAPI

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Staff-session auth default | implemented | `SessionAuthIsStaff`, `tests/test_permissions_auth.py`, safe default auth-admin write/read schema tests, session bootstrap route tests, installed-wheel generated-client CSRF/login/mutation/logout smoke, auth docs | More project-level examples |
| Custom auth and `auth=None` | implemented | `tests/test_permissions_auth.py`, auth contract tests, multi-auth mounted-route tests, auth-none site-route tests, and auth docs | More project-level examples |
| Typed error bodies | partial | `tests/test_error_contracts.py` covers representative runtime body validation for auth, permission, not-found, request-validation, form, inline, bulk-row, and protected-delete errors; Exception handlers, Ninja validation handler, runtime auth-error tests, built-in/model/custom route OpenAPI error maps including conditional `401` responses, concrete `ErrorResponse` OpenAPI examples for validation, permission, protected-delete, and deleted-object tree bodies, typed `ErrorMessage` and recursive `DeletedObject` schema components, semantic `ErrorItem`/`ErrorResponse` component snapshots, representative runtime body validation for auth, permission, not-found, request-validation, form, inline, bulk-row, and protected-delete errors, plus installed-wheel generated-client smoke validation for documented 401/404/400/422 responses against OpenAPI response schemas | Broader cross-route runtime edge coverage |
| Stable operation IDs/tags | partial | `tests/test_openapi_contracts.py` covers core site and model operation IDs/tags/security; Explicit and generated operation IDs plus semantic OpenAPI contract tests for core site/model routes and custom site/model routes, including multi-method uniqueness | Broader custom-route snapshot tests |
| Per-model request/response contracts | partial | `tests/test_openapi_contracts.py` covers model output, mutation, inline, bulk, action, changelist, form, history, and error response contract components; Output, write payload, generated create/update/bulk/action request examples, generated output/mutation/bulk/inline component examples including self-validating many-to-many/custom-field/output-override examples, closed default mutation response data, typed mutation inline operation results, constrained inline response maps keyed by registered inline identifiers, typed mutation/bulk response data, typed default action responses, declared custom action/add/change/delete hook response schemas, changelist/list-editing/form/inline response metadata components including typed `Row` cells, `CellMetadata`, list-editing row PKs, normalized fieldset layout schemas, typed action selection, bulk row, inline identifier request components, typed changelist query parameters including integer `pp` and boolean `all`/`_facets`, input-schema override metadata, and default custom-route JSON-object response schemas, `FieldDescription.attrs` examples, typed validator/widget/subwidget/input-format/select-date/combo-field form metadata components, typed prepopulated/radio form config maps, typed history identifiers/change messages, JSON-compatible scalar metadata fields, inline operation, multipart JSON part media hints plus required file parts, discriminated action payload/input/response, alternate custom-action success status responses, generic dead payload-schema absence, global action cache invalidation, auth-aware response-map schemas in OpenAPI, semantic OpenAPI diff tooling for release review, and installed-project OpenAPI consumer smoke coverage for typed changelist query-parameter schemas, constrained inline response maps, CSRF/login/mutation/logout, context/permissions/apps/history/autocomplete/view-on-site, and list/create/detail/patch/update/delete/bulk/action/form plus inline add/change operations with success/error response-schema validation | Broader Phase 6 model-route snapshots |

## Release Hardening

| Behavior | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Ruff/pytest local gates | implemented | `just lint`, `just test`, `just typecheck`, `just check`, `.github/workflows/ci.yml`; typecheck currently covers the package modules under `django_ninja_admin/` plus release and smoke scripts | Keep CI and local gates aligned as new modules are added |
| Test suite split | implemented | `tests/test_admin_api.py` has been retired; `tests/test_error_contracts.py` covers runtime error body shapes; `tests/test_crud_routes.py` covers CRUD/history/change-log smoke; `tests/test_package_contract.py` covers no-DRF package contracts; `tests/test_checks.py` and `tests/conftest.py` move all `test_admin_checks*` coverage out of `tests/test_admin_api.py`, including inline/layout/formset/parent-FK, relation-path list-display, action permission-hook, widget-option, related-loading, sorting, pagination, boolean, ordering, facet, date-hierarchy, expression-ordering, field-filter, custom-form, formfield-override, reverse-relation-widget, autocomplete-target, prepopulated-field, list-editable, form-layout, manual-through many-to-many, and schema-override checks with a shared `make_site` factory; `tests/test_openapi_schema.py` starts the OpenAPI split with docs/auth and semantic error-schema coverage while shared API fixtures live in `tests/conftest.py`; `tests/test_permissions_auth.py` covers auth errors, session bootstrap, permissions, safe `include_auth`, custom auth sequences, and default auth contracts; `tests/test_history.py` covers history filtering, pagination, object permissions, and page-scoped visibility; `tests/test_autocomplete.py` covers autocomplete pagination, hooks, relation target typing, source-field access, and page-scoped object permissions; `tests/test_actions_bulk.py` covers action dispatch, `delete_selected`, list-editable bulk updates, `_to_field` row identity, and bulk hooks; `tests/test_inlines.py` covers inline form-description metadata, inline Pydantic payload validation, inline formset constraints, inline permissions, rollback, duplicate/conflicting rows, and change-message text; `tests/test_changelist_filters.py` covers changelist facets, date hierarchy, bounded date filters, lookup validation, remote `to_field` related-list filters, null choices, direct lookup value normalization, empty filters, hidden simple filters, many-to-many empty filters, and related-only ordering; `tests/test_site.py` covers site registration/decorators, swapped-model skipping, autodiscover rollback, OpenAPI cache invalidation, custom admin routes, and context customization; `tests/test_changelist_core.py` covers mounted changelist list/detail, search, pagination/show-all/query-string state, callable/relation-path list display, ordering fallback, row permissions, action UI metadata, list-editable metadata, many-to-many search distinct behavior, search lookup suffixes, relation loading, prefetch hooks, changelist hooks, and paginator hooks; `tests/test_custom_forms.py` covers mounted custom form classes, response hooks, multivalue/temporal/scalar Pydantic normalization, disabled form fields, and formfield hooks; `tests/test_model_field_schemas.py` covers isolated model-field output/write schema behavior for image, email/URL, IP, JSON, many-to-many target examples, binary, regex, string length, numeric bounds, and step validators; `tests/test_schema_customization.py` covers output schema overrides, computed admin fields, non-auth password fields, typed override examples, and Ninja `register_field()` inference; `tests/test_model_routes.py` covers multipart/create request validation, direct delete details and object permissions, `_to_field` detail/changelist/update routes, action/bulk/autocomplete route smoke, and view-on-site variants; `tests/test_form_descriptions.py` covers change-form initial data, readonly/callable display metadata, layout fieldsets, raw-id/filter-vertical widget modes, multiwidget/select-date/file-path/combo/numeric-step metadata, and relation `limit_choices_to` metadata; `tests/test_file_fields.py` covers file/image JSON clear and rejection, required multipart file schemas, extension validation, multipart file and image uploads, image dimensions, and storage-without-URL metadata; `tests/test_form_field_schemas.py` covers richer Pydantic form-field schemas, input override schemas across parent/bulk/inline operations, and choice/typed-choice coercion; `tests/test_openapi_contracts.py` covers broad OpenAPI route/component examples, operation IDs, response maps, and semantic contract surfaces | Keep future coverage topic-scoped; avoid recreating aggregate test modules |
| Exception/dead-code cleanup | partial | Safe broad exception handlers are narrowed in deletion collection, related filters, form metadata, multivalue decompression, validator introspection, and Pydantic schema probes; schema/example and choice-field generation are extracted into `django_ninja_admin.utils.schema_examples` with direct tests; numeric/string/pattern/step constraint helpers are extracted into `django_ninja_admin.utils.schema_constraints` with direct tests; remaining broad handlers are import/autodiscover rollback and auth-callback boundaries | Continue extracting shared schema machinery and audit any future broad exception handlers as they are introduced |
| Package build/install smoke | implemented | `scripts/package_smoke.py`, `scripts/sample_project_smoke.py`, `just package-smoke`, `just sample-project-smoke`, `just sample-project-full`, `DJANGO_NINJA_ADMIN_SMOKE_DJANGO` installed-project Django pins, and `DJANGO_NINJA_ADMIN_WHEEL` reuse of prebuilt wheel artifacts | Expand sample project scenarios as parity grows |
| Parity report tooling | implemented | `scripts/parity_report.py`, `tests/test_parity_report.py`, and `just parity-report` | Expand into linked evidence checks as the matrix grows |
| OpenAPI diff tooling | implemented | `scripts/openapi_diff.py`, `tests/test_openapi_diff.py`, and `just openapi-diff` | Use reviewed OpenAPI artifacts before beta/stable release candidates |
| OpenAPI consumer smoke | implemented | `scripts/generated_client_smoke.py` and `just generated-client-smoke` exercise a built wheel from a clean Django project using operation IDs, advertised examples, schema-declared path/query parameters, typed changelist query-parameter schemas, encoded changelist query arguments, CSRF bootstrap, session login, authenticated mutation, logout, inline add/change payloads/results, full update, delete, site context, permissions, app-list, history, autocomplete, view-on-site, and documented success/error response schemas | Broaden to generated clients and additional route groups |
| Django version matrix | partial | `.github/workflows/ci.yml` covers Django 5.0, 5.1, 5.2, and 6.0 on Python 3.12+, including Python 3.13 and 3.14 lanes plus matrix-pinned installed-project sample smoke; the distribution job uploads one checked wheel reused by matrix smoke jobs | Confirm CI results; keep the matrix focused on Django 5+ and Python 3.12+ |
| PostgreSQL coverage | partial | Env-driven `tests/settings.py`, `just postgres-test`, PostgreSQL CI job | Confirm CI results and broaden database-specific edge cases |
| Copyright/license audit | implemented | `LICENSE`, `LICENSE-DJANGO`, `docs/copyright-audit.md` | Re-run before each release candidate and after substantial ports |
| Changelog/release checklist | implemented | `CHANGELOG.md`, `docs/release-checklist.md` | Expand release notes before each tag |
